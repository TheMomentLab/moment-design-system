import React from 'react';
import {PageHeader} from '../src/index.jsx';
// Layout values follow stories/Brand.shared.jsx: same width, sections and comparison panels.
export function BrandPage({title,description,children,heading=true}){return <div data-brand-format="lds" style={{display:'grid',gridTemplateColumns:'minmax(0, 1fr)',gap:'var(--space-8)',minWidth:0,maxWidth:1040}}>{heading&&<PageHeader headingLevel={1} eyebrow="Theme / Brand" title={title} description={description}/>} {children}</div>}
export function BrandSection({title,children}){return <section style={{display:'grid',gap:'var(--space-4)',minWidth:0}}><h2 style={{margin:0,color:'var(--color-semantic-label-strong)',fontSize:22}}>{title}</h2>{children}</section>}
export function BrandGrid({children}){return <div style={{display:'grid',gridTemplateColumns:'repeat(auto-fit, minmax(min(100%, 300px), 1fr))',gap:'var(--space-4)'}}>{children}</div>}
export function BrandPanel({children,theme,style}){return <article data-theme={theme} style={{display:'grid',gap:'var(--space-5)',padding:'var(--space-5)',border:'1px solid var(--color-semantic-line-normal-normal)',borderRadius:'var(--radius-lg)',background:'var(--color-semantic-background-elevated-normal)',color:'var(--color-semantic-label-normal)',minWidth:0,...style}}>{children}</article>}
export function BrandTable({heads,rows}){return <div style={{overflowX:'auto'}}><table style={{width:'100%',borderCollapse:'collapse',textAlign:'start',fontSize:'var(--fs-body)'}}><thead><tr>{heads.map(h=><th key={h} scope="col" style={{textAlign:'start',padding:'var(--space-3)',borderBottom:'1px solid var(--color-semantic-line-normal-normal)'}}>{h}</th>)}</tr></thead><tbody>{rows.map((row,i)=><tr key={i}>{row.map((cell,j)=><td key={j} style={{padding:'var(--space-3)',borderBottom:'1px solid var(--color-semantic-line-normal-neutral)',verticalAlign:'top',overflowWrap:'anywhere'}}>{cell}</td>)}</tr>)}</tbody></table></div>}
